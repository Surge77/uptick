# Normalize line endings to LF in the repository. Prettier enforces LF, so a
# CRLF-committed file would fail `format:check` on the Linux CI runner.
* text=auto eol=lf

pnpm-lock.yaml linguist-generated=true
*.png binary
*.jpg binary
*.ico binary
